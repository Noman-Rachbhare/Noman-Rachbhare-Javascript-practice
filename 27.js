const bike={
    color:"Blue",
    price:700000,
    model:"Abc",
    "aveg speed":30

}
// console.log(Object.keys(bike));
for (let key of Object.keys(bike) ) {
    // console.log(bike[key]);
    console.log(bike[key]);
    
}